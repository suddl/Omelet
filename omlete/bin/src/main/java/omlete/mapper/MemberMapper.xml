<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="omlete.mapper.MemberMapper">
	<insert id="insertMember">
		<selectKey resultType="int" keyProperty="member_no" order="BEFORE">
			select member_seq.nextval from dual
		</selectKey>
		insert into member values(#{memberNo}, #{memberId}, #{memberPasswd}, #{memberName}, #{memberPhone}, #{memberBirth}, #{memberEmail}, #{memberLevel}, 
		#{memberBadge}, #{memberStatus}, #{memberPoint}, #{memberFavorite1}, #{memberFavorite2}, #{memberFavorite3}, #{memberReport}, #{memberNickname})
	</insert>
	
	<select id="selectMember" resultType="Member">
		select * from member where m_id=#{mId}
	</select>
</mapper>