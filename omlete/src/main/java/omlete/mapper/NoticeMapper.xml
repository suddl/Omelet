<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="omlete.mapper.NoticeMapper">
	<insert id="insertNotice">
		<selectKey resultType="int" keyProperty="notice_no" order="BEFORE">
			select notice_seq.nextval from dual
		</selectKey>
		insert into notice values(#{noticeNo}, #{noticeTitle}, #{noticeContent}, #{noticeCount},
			#{noticeDate}, #{noticeStatus}, #{noticeTerm}, #{noticeImage})
	</insert>
	
	

	
	
	
	<!-- 검색 -->
	<select id="search" parameterType="hashMap" resultType="BoardVO">
	    select
	    notice_title, notice_content
	    from notice
	    <if test='searchType.equals("notice_title")'>
	        WHERE notice_title LIKE concat('%', #{keyword}, '%')
	    </if>
	    <if test='searchType.equals("notice_content")'>
	    	WHERE notice_content LIKE concat('%', #{keyword}, '%')
	    </if>
	    order by notice_no desc
	</select>
	
	<!-- 수정 -->
	<update id="update" parameterType="board">
		update notice 
		set notice_title = #{noticeTitle},
		notice_content = #{noticeContent},
		notice_date = #{noticeDate},
		notice_status = #{noticeStatus},
		notice_term = #{noticeTerm},
		notice_image = #{noticeImage},
		where notice_no = #{noticeNo}
	</update>
	
	<!-- 상세보기 -->
	<select id="noticeboardDetailData" resultType="NoticeBoardVO" parameterType="number">
    select notice_no, notice_title, notice_content, notice_date, notice_term, notice_image
    from notice where notice_no=#{noticeNo}
	</select>
	
	<!-- 삭제 -->
	<delete id="deleteNotice">
		delete from notice where notice_no=#{noticeNo}
	</delete>
	
	<!-- 레코드수 조회 -->
	<select id="selectNoticeCount" resultType="int">
		select count(*) from notice
	</select>
	
	<!-- 전체리스트 -->
	<select id="selectNoticeList" resultType="Notice">
    	select * from (select rownum rn, notice.* from (select notice_no, notice_title, notice_content,
    		notice_count, notice_date, notice_status, notice_term, notice_image
            from Notice order by notice_no desc) notice) where rn between #{startRow} and #{endRow}
	</select>
</mapper>